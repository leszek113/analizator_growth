version: '3.8'

services:
  analizator-growth:
    image: leszek113/analizator-growth:v1.3.0
    container_name: analizator-growth-1.3.0
    ports:
      - "5002:5002"
    volumes:
      # Persystencja bazy danych
      - ./data:/app/data
      # Logi aplikacji
      - ./logs:/app/logs
      # Pliki konfiguracyjne
      - ./config:/app/config
      # Credentials Google Sheets
      - ./secrets:/app/secrets
    environment:
      - FLASK_ENV=production
      - PYTHONPATH=/app
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:5002/"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s

volumes:
  data:
  logs:
